#!/usr/bin/env node
// @yourtechtribe-labs/koncept-cli
// CLI dispatcher — populated at T26+

import { VERSION } from '@yourtechtribe-labs/koncept-core'

console.log(`koncepto cli skeleton (core ${VERSION}) — commands registered at T26+`)
