packages:
  - "packages/*"

# Security: only allow build scripts from explicitly trusted packages.
# Default-deny (post npm Sept-2025 supply-chain attack).
# See knowledge/resources/dev/domain/security/SECURITY-NPM-2025.md
onlyBuiltDependencies:
  - esbuild   # vitest dep, well-known + maintained

